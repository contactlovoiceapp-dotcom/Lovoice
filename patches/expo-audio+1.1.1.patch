diff --git a/node_modules/expo-audio/ios/AudioModule.swift b/node_modules/expo-audio/ios/AudioModule.swift
index 938f985..c451c58 100644
--- a/node_modules/expo-audio/ios/AudioModule.swift
+++ b/node_modules/expo-audio/ios/AudioModule.swift
@@ -568,6 +568,9 @@ public class AudioModule: Module {
 #else
         categoryOptions.insert(.allowBluetooth)
 #endif
+        if !mode.shouldRouteThroughEarpiece {
+          categoryOptions.insert(.defaultToSpeaker)
+        }
       }
 #endif
 
@@ -592,7 +595,12 @@ public class AudioModule: Module {
         return
       }
       let hasActivePlayers = self.registry.allPlayers.values.contains { $0.isPlaying }
-      if !hasActivePlayers {
+#if os(iOS)
+      let hasActiveRecorders = self.registry.allRecorders.values.contains { $0.isRecording }
+#else
+      let hasActiveRecorders = false
+#endif
+      if !hasActivePlayers && !hasActiveRecorders {
         do {
           try AVAudioSession.sharedInstance().setActive(false, options: [.notifyOthersOnDeactivation])
         } catch {
diff --git a/node_modules/expo-audio/ios/AudioRecords.swift b/node_modules/expo-audio/ios/AudioRecords.swift
index f1759cd..19b7402 100644
--- a/node_modules/expo-audio/ios/AudioRecords.swift
+++ b/node_modules/expo-audio/ios/AudioRecords.swift
@@ -6,6 +6,7 @@ struct AudioMode: Record {
   @Field var allowsRecording: Bool = false
   @Field var shouldPlayInBackground: Bool = false
   @Field var allowsBackgroundRecording: Bool = false
+  @Field var shouldRouteThroughEarpiece: Bool = false
 }
 
 enum InterruptionMode: String, Enumerable {
